This is a Tauri project with NextJS as the front-end framework. 

## Getting Started

First, run install the necessary packages.:

```bash
npm i install
```

Next, start the development development server:

```bash
npx tauri dev
```
